# Core API
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.1

# Database
# NOTE: supabase<2.3 pins httpx<0.25 which conflicts with anthropic>=0.84
# (which requires httpx>=0.25). 2.9.1 is the sweet spot: uses httpx>=0.26
# and has no breaking changes in the client API we use (auth + from_).
supabase==2.9.1

# OpenAI (still used for embeddings)
openai==1.30.0

# Anthropic (Claude Haiku for chat/generation)
anthropic>=0.84.0

# Auth
python-jose[cryptography]==3.4.0
bcrypt==4.2.0

# HTTP
requests==2.32.0

# Utils
pydantic==2.7.0
# chromadb 1.3.5 matches the on-disk schema at data/vectordb/chroma.sqlite3 —
# 0.4.24 queries `collections.topic` which was dropped in chromadb 0.5, so
# reading the current DB with 0.4.24 crashes with
#   sqlite3.OperationalError: no such column: collections.topic.
# Keep numpy<2 — chromadb 1.x works with numpy 2, but pinning matches the
# local env that created the DB and avoids surprise float-type changes.
numpy<2.0
chromadb==1.3.5
rapidfuzz
beautifulsoup4==4.12.3
